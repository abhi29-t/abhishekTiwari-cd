import { makeStyles } from "@mui/styles";

export const useStyles: any = makeStyles({
  root: {
    height: "84vh",
  },
  filtersContainer: {
    height: "8rem",
  },
  filtersResult: {
    height: "100%",
  },
});
